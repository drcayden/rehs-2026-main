# Exposes the pod inside the cluster on port 80 -> container 8501.
apiVersion: v1
kind: Service
metadata:
  name: chatbot
  namespace: rehs-2026-chatbot
spec:
  selector: {app: chatbot}
  ports:
  - port: 80
    targetPort: 8501
  type: ClusterIP
